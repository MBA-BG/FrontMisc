import { useState } from "react";

import AccordionPanel from "./AccordionPanel/AccordionPanel";

const FAQ = (props) => {

  return (
    <div>
        <AccordionPanel/>
     </div>
  );
};

export default FAQ;
